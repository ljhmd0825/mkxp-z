name: Manual Embedded aarch64 Build

on:
  workflow_dispatch: # Actions 탭에서 수동으로 버튼을 눌러 실행합니다.

jobs:
  build-aarch64-focal:
    name: Ubuntu 20.04 aarch64 (Embedded)
    runs-on: ubuntu-latest
    container: ubuntu:20.04 # GCC 9 및 Glibc 2.31 환경 강제
    
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1. 의존성 및 빌드 캐싱
      - name: Cache APT and Dependencies
        uses: actions/cache@v4
        with:
          path: |
            /var/cache/apt/archives
            linux/downloads
            linux/build-arm64
          key: build-cache-focal-arm64-${{ hashFiles('linux/Makefile', 'linux/target-arm64.sh') }}
          restore-keys: |
            build-cache-focal-arm64-

      # 2. APT 저장소 설정 및 도구 설치
      - name: Setup Multi-Arch Repositories
        run: |
          export DEBIAN_FRONTEND=noninteractive
          
          # 1. 아키텍처 추가 및 저장소 분리 (이 부분은 이제 완벽합니다)
          sed -i 's/deb http/deb [arch=amd64] http/g' /etc/apt/sources.list
          echo "deb [arch=arm64] http://ports.ubuntu.com/ focal main restricted universe multiverse" > /etc/apt/sources.list.d/arm64.list
          echo "deb [arch=arm64] http://ports.ubuntu.com/ focal-updates main restricted universe multiverse" >> /etc/apt/sources.list.d/arm64.list
          echo "deb [arch=arm64] http://ports.ubuntu.com/ focal-security main restricted universe multiverse" >> /etc/apt/sources.list.d/arm64.list
          dpkg --add-architecture arm64
          apt-get update

          # 2. [수정] CMake 최신 버전 설치 (GPG 키 에러 방지를 위해 직접 다운로드)
          apt-get install -y wget
          wget https://github.com/Kitware/CMake/releases/download/v3.26.4/cmake-3.26.4-linux-x86_64.sh
          chmod +x cmake-3.26.4-linux-x86_64.sh
          ./cmake-3.26.4-linux-x86_64.sh --skip-license --prefix=/usr/local
          ln -sf /usr/local/bin/cmake /usr/bin/cmake

          # 3. Meson & Ninja 최신 버전 설치 (0.56.0 요구사항 해결)
          apt-get install -y python3-pip python3-setuptools
          pip3 install --upgrade pip
          pip3 install meson ninja
          
          # 3. 필수 패키지 설치 (Xext 등 아까 추가한 것들 포함)
          apt-get install -y -o Dpkg::Options::="--force-confdef" -o Dpkg::Options::="--force-confold" \
            git build-essential meson autoconf automake libtool pkg-config ruby bison xxd \
            gcc-aarch64-linux-gnu g++-aarch64-linux-gnu \
            zlib1g-dev:arm64 libbz2-dev:arm64 libgl1-mesa-dev:arm64 libasound2-dev:arm64 \
            libpulse-dev:arm64 libx11-dev:arm64 libexpat1-dev:arm64 libdbus-1-dev:arm64 \
            libxext-dev:arm64 libxrandr-dev:arm64 libxcursor-dev:arm64 libxinerama-dev:arm64 \
            libxi-dev:arm64 libxss-dev:arm64 libxkbcommon-dev:arm64 libdrm-dev:arm64 libgbm-dev:arm64

          # 4. Autoconf 2.71 설치
          wget https://ftp.gnu.org/gnu/autoconf/autoconf-2.71.tar.gz
          tar -xzf autoconf-2.71.tar.gz && cd autoconf-2.71 && ./configure --prefix=/usr && make -j$(nproc) && make install
          cd .. && rm -rf autoconf-2.71*

      # 3. MKXP-Z 전용 라이브러리 빌드
      - name: Build External Libraries
        if: steps.cache-apt-and-dependencies.outputs.cache-hit != 'true'
        run: |
          cd linux
          export ARCH_MKXPZ=arm64
          export ARCH_GCC=aarch64-linux-gnu
          export MKXPZ_PREFIX=$GITHUB_WORKSPACE/linux/build-arm64
          
          # [중요] 링커가 arm64 경로를 우선하지 않도록 PKG_CONFIG 설정을 정밀하게 조정
          export PKG_CONFIG_PATH=$MKXPZ_PREFIX/lib/pkgconfig:/usr/lib/aarch64-linux-gnu/pkgconfig
          
          # [핵심] DBus 빌드 시 툴 빌드를 건너뛰거나, 시스템 패키지를 찾도록 유도
          # 만약 Makefile이 허용한다면 아래처럼 dbus를 명시적으로 처리하거나
          # 아예 에러 무시하고 강행(make -k)할 수도 있지만, 우선은 환경 변수 격리가 우선입니다.
          
          export GEM_PATH=""
          export GEM_HOME=""
          export RUBYOPT="-Eutf-8"
          
          if [ -f /usr/lib/ruby/vendor_ruby/rubygems/defaults/operating_system.rb ]; then
            rm /usr/lib/ruby/vendor_ruby/rubygems/defaults/operating_system.rb
          fi

          # 병렬 빌드(-j)를 끄고 순차적으로 진행해서 로그를 더 명확히 봅니다.
          make

      # 4. mkxp-z 본체 빌드
      - name: Build mkxp-z
        run: |
          cd linux
          # 경로 변수를 절대 경로로 고정
          export MKXPZ_PREFIX=$GITHUB_WORKSPACE/linux/build-arm64
          
          # 2. Meson 크로스 파일 생성 (pkg-config 경고 수정본)
          cat <<EOF > target-arm64.meson
          [binaries]
          c = 'aarch64-linux-gnu-gcc'
          cpp = 'aarch64-linux-gnu-g++'
          ar = 'aarch64-linux-gnu-ar'
          strip = 'aarch64-linux-gnu-strip'
          pkg-config = 'pkg-config'

          [host_machine]
          system = 'linux'
          cpu_family = 'aarch64'
          cpu = 'armv8-a'
          endian = 'little'

          [properties]
          pkg_config_libdir = ['/usr/lib/aarch64-linux-gnu/pkgconfig', '$MKXPZ_PREFIX/lib/pkgconfig']
          EOF

          cd ..
          rm -rf build-arm64-final
          
          # 4. Meson 실행 (변수 대신 직접 경로 주입)
          meson setup build-arm64-final \
            --cross-file linux/target-arm64.meson \
            --prefix="$GITHUB_WORKSPACE/linux/build-arm64" \
            -Druby_decl_dir="$GITHUB_WORKSPACE/linux/build-arm64/lib" \
            -Dworkdir_use_etc=false

          ninja -C build-arm64-final install

      # 5. 빌드 결과물 업로드 (경로 수정)
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mkxp-z-focal-arm64-artifact
          # 최종 실행 파일이 설치된 경로를 지정해야 합니다.
          path: linux/build-arm64/bin/mkxp-z
